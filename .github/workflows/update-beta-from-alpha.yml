name: Create Beta Release on PR to Beta

on:
  pull_request:
    branches:
      - beta
    types:
      - closed  # Nur wenn der PR gemerged wurde
  workflow_dispatch:

jobs:
  create-beta-release:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest

    steps:
      - name: Checkout alpha branch
        uses: actions/checkout@v4
        with:
          ref: alpha
          fetch-depth: 0
          submodules: recursive
          token: ${{ secrets.PERSONAL_TOKEN }}

      - name: Get latest version tag
        id: get_version
        run: |
          LATEST_TAG=$(git describe --tags --abbrev=0 || echo "v0.0.0")
          echo "Latest tag: $LATEST_TAG"
          VERSION=${LATEST_TAG#v}
          IFS='.' read -r MAJOR MINOR PATCH <<< "$VERSION"
          PATCH=$((PATCH + 1))
          NEXT_VERSION="v$MAJOR.$MINOR.$PATCH-beta"
          echo "RELEASE_TAG=$NEXT_VERSION" >> $GITHUB_ENV
          echo "RELEASE_TAG=$NEXT_VERSION" >> $GITHUB_OUTPUT
          echo "LATEST_TAG=$LATEST_TAG" >> $GITHUB_OUTPUT

      - name: Create release zip
        run: |
          zip -r ddpc-beta.zip . -x "*.git*" -x "ddpc-beta.zip"

      - name: Upload release zip as artifact
        uses: actions/upload-artifact@v4
        with:
          name: ddpc-beta
          path: ddpc-beta.zip

      - name: Create GitHub Pre-release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.get_version.outputs.RELEASE_TAG }}
          name: Release ${{ steps.get_version.outputs.RELEASE_TAG }}
          body: |
            Automatic beta release created from merged PR to beta.
            Version bumped from ${{ steps.get_version.outputs.LATEST_TAG }}.
          prerelease: true
          files: ddpc-beta.zip
        env:
          GITHUB_TOKEN: ${{ secrets.PERSONAL_TOKEN }}
